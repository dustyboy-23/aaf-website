node_modules/
.next/
.env.local
.env*.local
.vercel

# One-time Ghost export artifacts (content lives in content/posts/ as MDX)
_migration/

# Build outputs
tsconfig.tsbuildinfo
next-env.d.ts

# Lead magnet dev previews (regenerated by snap-all.mjs, not for commit)
lead-magnet/preview-*.png
lead-magnet/page-*.png

# Newsletter stub store (subscriber data, never commit)
data/subscribers.jsonl
